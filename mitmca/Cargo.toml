[package]
name = "mitmca"
version = "0.1.0"
authors = ["quininer <quininer@live.com>"]
edition = "2018"

[dependencies]
failure = "0.1"
publicsuffix = { version = "1", default-features = false }
openssl = "0.10"
lru_time_cache = "0.8"
lazy_static = "1"
if_chain = "0.1"

[build-dependencies]
publicsuffix = { version = "1", default-features = false }
reqwest = "0.9"
